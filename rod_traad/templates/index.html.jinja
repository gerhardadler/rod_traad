<!DOCTYPE html>
<html lang="no">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/static/images/favicon.ico">
    <link rel="stylesheet" href="/static/style/global.css">
    <link rel="stylesheet" href="/static/style/button.css">
    <link rel="stylesheet" href="/static/style/index.css">
    <script src="/static/script/index.js" defer></script>
    <title>rod_traad</title>
</head>

<body>
    <main>
        <div class="content">
            <h1>19. april 2025</h1>
            <div id="puzzle">
                {# <div class="solved solution-1">
                    <h3>Løsning 1</h3>
                    <p>ord1, ord2, ord3, ord4</p>
                </div>
                <div class="solved solution-2">
                    <h3>Løsning 2</h3>
                    <p>ord1, ord2, ord3, ord4</p>
                </div> #}
                {% for row in puzzle.grid %}
                {% set outer = loop %}
                {% for word in row %}
                {% set index = outer.index0 * 4 + loop.index %}
                <label for="word{{ index }}" class="word">
                    <input type="checkbox" class="visually-hidden" id="word{{ index }}" autocomplete="off">
                    <span>{{ word }}</span>
                </label>
                {% endfor %}
                {% endfor %}
            </div>
            <button id="submit" class="button neutral">Bekreft</button>
        </div>
    </main>
    <script>
        const solutions = JSON.parse('{{ puzzle.model_dump(mode="json").solutions | tojson }}');
    </script>
</body>

</html>